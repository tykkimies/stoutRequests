# Database Configuration
DATABASE_URL=postgresql://stout:Yyxd7fku!@localhost:5432/stout_requests

# Application Security
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Environment
ENVIRONMENT=development

# Note: API keys and service URLs are now configured securely through the web interface
# Visit http://localhost:8000/setup on first run to configure:
# - Plex server connection
# - TMDB API key  
# - Radarr/Sonarr integration (optional)
#
# This approach keeps sensitive credentials out of files and allows
# secure web-based configuration by administrators only.